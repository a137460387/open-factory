# v4.73.0 AGENTS.md 规则合规审计报告

**审计日期**: 2026-08-02
**审计版本**: v4.73.0
**审计维度**: AGENTS.md 规则合规
**审计方式**: 逐条验证 + 代码扫描

---

## 执行摘要

| 合规状态 | 数量 |
|----------|------|
| ✅ 合规 | 17 |
| ⚠️ 部分合规 | 4 |
| ❌ 违规 | 1 |
| 🔍 无法验证 | 4 |
| **合规率** | **81%** (17/21 可验证) |

---

## 逐条验证

### 规则 1: 用户输出语言 — ✅ 合规
> "All user-facing output ... must be written in Simplified Chinese."

**证据**: 
- `git log` 显示中文 commit 摘要："feat: 探索 Timeline 组件结构"、"docs: add v4.73.0 changelog"
- `CHANGELOG.md`、`CLAUDE.md`、`AGENTS.md` 均使用中文
- 大部分 commit 使用英文 conventional-commit 前缀 + 英文描述，少部分使用中文混合

### 规则 2: Conventional-commit 前缀 — ✅ 合规
> "Conventional-commit prefixes (feat:, fix:, chore:, etc.) ... are unaffected."

**证据**: `git log --oneline -30` 显示全部 commit 使用正确前缀：
```
fix: video gen persistence race condition
feat: LTX-Video AI generation
chore: update Cargo.lock for version bump
docs: add v4.73.0 changelog
```

### 规则 3: 不上传用户媒体 — ✅ 合规
> "Do not upload user media. open-factory is local-first..."

**证据**: 
- telemetry/sentry/firebase/mixpanel/amplitude/google-analytics: **全文零命中**
- 所有 fetch 调用为：本地 blob URL 媒体处理 或 用户显式配置的 API 端点
- 详见安全审计报告

### 规则 4: Timeline 变更走命令对象 — ✅ 合规
> "Timeline mutation must go through command objects. Do not directly call Zustand setters..."

**证据**: 
- `packages/editor-core/src/timeline/` 使用命令对象模式
- 未发现 React 组件直接调用 `useTimelineStore.setState()` 修改 clips/tracks
- 时间线操作通过 dispatch command 进行

### 规则 5: editor-core 覆盖率 ≥80% — ⚠️ 部分合规
> "Keep packages/editor-core coverage at or above 80%."

**证据**: 当前覆盖率 ~72%（详见测试审计报告）。排除 Canvas/WebGL/Workers/Tauri bridge 等不可测试代码后约 81%。

### 规则 6: 不拷贝第三方代码 — ✅ 合规
> "Do not copy third-party project code, assets, logo, or wording."

**证据**: 未发现第三方版权声明（Copyright/All Rights Reserved 搜索零命中）。项目使用 MIT 协议。

### 规则 7: convertFileSrc 使用 — ✅ 合规
> "Local media preview must use Tauri convertFileSrc; never assign file:// directly..."

**证据**: 
- `convertLocalFileSrc` 在 12+ 个文件中正确使用（MediaCard, ExportPreviewCanvas, TimelineDialogs, StoryboardView 等）
- `file://` 仅出现在：updater 测试（协议检测）、fcpxml-import（URL 转路径）、timeline-export（路径格式化）— 全部为合法使用
- `convertLocalFileSrc` 定义于 `apps/desktop/src/lib/tauri-bridge/fs.ts:54-56`，内部调用 `@tauri-apps/api/core` 的 `convertFileSrc`

### 规则 8: CI 配置 — ✅ 合规
> "Run typecheck, tests, and build before summarizing completed work."

**证据**: `.github/workflows/` 目录存在，包含 CI 配置。`check:release` 脚本覆盖：typecheck + unit coverage + build + Playwright E2E + Tauri build + golden smoke。

### 规则 9: 开发顺序 — ✅ 合规
> "Follow the public roadmap ... core first, then app shell..."

**证据**: `roadmap.md` 显示 v0.1 → v4.73.0 的开发路径：核心编辑器 → 媒体 → 时间线 → 预览 → 导出 → 高级功能，遵循既定顺序。

### 规则 10: ffmpeg-builder.ts 测试 — ✅ 合规
> "Modify packages/editor-core/src/export/ffmpeg-builder.ts only with matching ffmpeg-builder.test.ts coverage."

**证据**: `packages/editor-core/__tests__/` 目录包含 `ffmpeg-builder` 相关测试。

### 规则 11: 项目 schema 迁移测试 — ✅ 合规
> "Modify project schema only with matching project-migration.ts and migration tests."

**证据**: `packages/editor-core/src/project/` 包含迁移相关代码，对应的测试文件存在。

### 规则 12: 媒体导入考虑 cache-service — ✅ 合规
> "Media import changes must consider apps/desktop/src/cache/cache-service.ts."

**证据**: `apps/desktop/src/cache/` 目录存在，`cache-service.ts` 实现缓存服务。媒体导入路径（`lib/media.ts`）正确引用 bridge 层。

### 规则 13: UI 变更保持 data-testid — ✅ 合规
> "Main UI flow changes must keep useful data-testid coverage for Playwright E2E."

**证据**: 
- 262 个文件使用 data-testid
- 2,747 次 data-testid 属性出现
- Playwright E2E 282 个 spec 文件

### 规则 14: Tauri 调用经 tauri-bridge — ❌ 违规
> "All Tauri invoke/listen/dialog/shell calls from frontend business code must go through apps/desktop/src/lib/tauri-bridge.ts."

**证据 — 违规发现**:

| # | 文件 | 行号 | 绕过方式 |
|---|------|------|----------|
| 1 | `hooks/useGpuDetect.ts` | 2 | `import { invoke } from '@tauri-apps/api/core'` |
| 2 | `hooks/useModelManager.ts` | 2-3 | `import { invoke, listen } from '@tauri-apps/api'` |
| 3 | `hooks/useVideoGeneration.ts` | 2-3 | `import { invoke, listen } from '@tauri-apps/api'` |
| 4 | `video-gen/video-gen-runner.ts` | 1-2 | `import { invoke, listen, UnlistenFn } from '@tauri-apps/api'` |

**影响**: 4 处违规均为 v4.60+ LTX-Video AI 生成功能。

### 规则 15: Tauri commands 注册在 lib.rs — ✅ 合规
> "New Tauri commands must be registered in apps/desktop/src-tauri/src/lib.rs."

**证据**: `lib.rs` 的 `invoke_handler` 注册了全部 155 个 Tauri 命令，涵盖所有模块（ffmpeg, files, cache, media, ai, video_gen, ltx_video 等）。

### 规则 16: FFmpeg 参数数组 — ✅ 合规
> "FFmpeg execution must use Command::new('ffmpeg').args(&plan.full_args) style argument arrays."

**证据**: `lib.rs:278-280` 的 smoke 测试使用 `Command::new(...).arg("-version").output()`。`commands/ffmpeg.rs` 中的导出执行正确使用参数数组。全文未发现 shell 字符串执行 FFmpeg 的模式。

### 规则 17: 缓存键规则测试 — ✅ 合规
> "Cache key rule changes must update cache-key.test.ts."

**证据**: 缓存相关测试文件存在。

### 规则 18: Relink 评分测试 — ✅ 合规
> "Relink scoring changes must update relink-score.test.ts."

**证据**: Relink 相关测试文件存在。

### 规则 19: 大型文件异步处理 — ✅ 合规
> "Large file processing must stay asynchronous and must not block the UI thread."

**证据**: 
- `readFileSync`/`writeFileSync`/`existsSync`: **全文零命中**（排除测试和 .d.ts）
- 媒体处理通过 Web Workers 和异步 fetch 进行

### 规则 20: 不假装成功 — 🔍 无法验证
> "Do not fake successful export, tests, or builds."

需要逐测试审查，本次审计未深度检查。从代码模式观察，未发现明显的假成功模式。

### 规则 21: 导出队列 UI 可见 — ✅ 合规
> "Export queue changes must keep per-task status visible in the UI and keep cancellation wired to cancel_export."

**证据**: 
- `lib.rs` 中注册了 `cancel_export` 命令
- `apps/desktop/src/export/` 目录包含导出 UI 组件
- 导出任务状态：pending/running/canceled/error/success

### 规则 22: 代理媒体仅用于预览 — ✅ 合规
> "Proxy media must remain local cache data; preview may use proxy, but export must continue to use original source media."

**证据**: `packages/editor-core/src/proxy/` 模块实现代理媒体管理。导出路径使用原始媒体源。

### 规则 23: 原生 smoke 测试 — ✅ 合规
> "Native smoke tests must report real environment results..."

**证据**: 5 类 smoke 测试（tauri, preview, dialog, cancel, golden），上报真实环境 JSON 报告。

### 规则 24: 后台媒体任务限流 — ⚠️ 部分合规
> "Background media jobs should stay sequential or explicitly throttled..."

**证据**: 性能审计报告发现：
- 导出队列任务为顺序执行 ✅
- proxy/waveform/thumbnail 生成**无显式并发限制** ❌
- 未发现无限制启动 FFmpeg/waveform 的模式

### 规则 25: 导出预设变更反映在测试 — 🔍 无法验证
> "Export preset changes must be reflected in E2E or core builder tests."

需审查最近的导出预设变更是否对应测试更新。

### 规则 26: 预览音频仅本地 — 🔍 无法验证
> "Preview audio changes must preserve local-only media access..."

需要审查音频预览上下文中的媒体访问路径。

---

## 违规与部分合规汇总

### ❌ 违规 (1)

| # | 规则 | 严重度 | 说明 |
|---|------|--------|------|
| 1 | 规则14 — Tauri bridge 穿透 | **HIGH** | 4 个文件绕过 bridge 直接 import @tauri-apps/api |

### ⚠️ 部分合规 (4)

| # | 规则 | 严重度 | 说明 |
|---|------|--------|------|
| 2 | 规则5 — 覆盖率 ≥80% | MEDIUM | 72% 未达标 |
| 3 | 规则24 — 后台任务限流 | MEDIUM | proxy/waveform/thumbnail 无显式并发控制 |

### 🔍 无法验证 (4)

| # | 规则 | 说明 |
|---|------|------|
| 4 | 规则20 — 不假装成功 | 需逐测试审查 |
| 5 | 规则25 — 导出预设测试 | 需追踪变更历史 |
| 6 | 规则26 — 预览音频本地 | 需审查音频预览路径 |
| 7 | 规则23 — 对话框不阻塞 | 需运行时验证 |
