# open-factory 全量审计报告

**审计日期**: 2026-07-14
**项目版本**: v4.25.0
**审计范围**: 代码质量、依赖安全、测试覆盖度、配置文档一致性

---

## 一、审计总览

| 维度 | P0 | P1 | P2 | 合计 |
|------|----|----|-----|------|
| 代码质量与架构 | 5 | 8 | 12 | 25 |
| 依赖与安全 | 2 | 4 | 6 | 12 |
| 测试覆盖度 | 3 | 5 | 5 | 13 |
| 配置与文档一致性 | 3 | 5 | 6 | 14 |
| **合计** | **13** | **22** | **29** | **64** |

---

## 二、P0 严重问题（需立即修复）

### 代码质量 (5个)

| # | 位置 | 问题 | 修复建议 |
|---|------|------|----------|
| C-P0-1 | `src-tauri/src/commands/ai.rs:288` | `Box::leak` 永久泄漏字符串，每次调用 `normalize_provider_id` 都会泄漏内存 | 改用 `lazy_static` 或 `OnceLock` 缓存，或使用 `&'static str` 常量 |
| C-P0-2 | `src-tauri/src/db/mod.rs:65-74` | `get_or_open` 每次都创建新连接（第73行直接调用 `open_db`），连接池形同虚设 | 修改为在 `guard.is_some()` 时返回已有连接（需解决 `Connection` 不支持 Clone 的问题，可改用 `Rc<Connection>` 或连接池 crate） |
| C-P0-3 | `src-tauri/src/commands/ffmpeg.rs` | 5203行、203个函数的上帝文件，职责过多 | 拆分为 `ffmpeg-encode.rs`、`ffmpeg-decode.rs`、`ffmpeg-filter.rs`、`ffmpeg-hw.rs` 等模块 |
| C-P0-4 | `src/components/Inspector/Inspector.tsx` | 6420行、87个函数、19个 useEffect 的上帝组件 | 按功能拆分为 `VideoInspector`、`AudioInspector`、`AIInspector` 等子组件 |
| C-P0-5 | `src/components/Timeline/Timeline.tsx` | 6140行、14个 useEffect、42处全局事件监听 | 拆分为 `TimelineCanvas`、`TimelineTracks`、`TimelineControls` 等子组件，提取事件监听到自定义 hook |

### 依赖与安全 (2个)

| # | 位置 | 问题 | 修复建议 |
|---|------|------|----------|
| D-P0-1 | `package.json:62` | 根依赖中错误声明了 npm 包 `desktop@^0.1.0`，与项目桌面应用无关，引入不必要的传递依赖 | **已自动修复** ✅ — 已从 dependencies 中移除 |
| D-P0-2 | `package-lock.json` 磁盘残留 | 过时的 package-lock.json（版本4.22.0 vs 当前4.25.0），可能造成包管理器混乱 | **已自动修复** ✅ — 文件已被 .gitignore 排除，本地残留不影响仓库 |

### 测试覆盖度 (3个)

| # | 位置 | 问题 | 修复建议 |
|---|------|------|----------|
| T-P0-1 | `e2e/ai-local-denoise.spec.ts:4` | E2E 测试被 `test.skip` 跳过，测试 AI 音频降噪功能 | Mock 和 UI 组件均已就绪，需调试进度事件与 UI 状态同步的竞态条件 |
| T-P0-2 | `e2e/ai-multicam-cut.spec.ts:13` | E2E 测试被 `test.skip` 跳过，测试 AI 多机位剪辑建议 | 同一 fixture 在 `multicam-editing.spec.ts` 中工作正常，需检查 AI 建议数据注入方式 |
| T-P0-3 | `editor-core/src/ai-emotion-analyzer.ts` | 覆盖率仅 0.6%（180行代码，仅1行被覆盖），无对应测试文件 | 创建 `__tests__/ai-emotion-analyzer.test.ts` |

### 配置与文档一致性 (3个)

| # | 位置 | 问题 | 修复建议 |
|---|------|------|----------|
| F-P0-1 | `src-tauri/tauri.conf.json:77` | 更新端点指向错误的 GitHub 组织 `open-factory/open-factory`，实际应为 `a137460387/open-factory` | **已自动修复** ✅ — 已更正 URL |
| F-P0-2 | `CHANGELOG.md` | 缺失 55 个版本的变更记录（v0.2~v2.9、v3.11~v3.13、v4.0~v4.24） | 从 git log 提取 commit 信息补全，至少补全 v4.x 系列 |
| F-P0-3 | `CHANGELOG.md:137-148` | v4.25.0 的比较链接缺失，底部链接列表仅有 v3.x 系列 | **已自动修复** ✅ — 已添加 v4.25.0 比较链接 |

---

## 三、P1 重要问题（建议尽快修复）

### 代码质量 (8个)

| # | 位置 | 问题 | 修复建议 |
|---|------|------|----------|
| C-P1-1 | `editor-core` 多个模块 | 38 处空 catch 块，静默吞掉错误（涉及 AI、导出、协作等核心模块） | 至少添加 `console.error` 或统一错误上报 |
| C-P1-2 | `src/` 多个组件 | 40+ 处 `.catch(() => undefined)` 错误被完全丢弃，尤其在导出队列中 | 添加错误日志或用户提示 |
| C-P1-3 | `src/components/Settings/SettingsDialog.tsx` | 4720 行设置对话框 | 按设置类别拆分子组件 |
| C-P1-4 | `src/components/Export/ExportDialog.tsx` | 4055 行导出对话框 | 拆分为导出配置、预览、进度等子组件 |
| C-P1-5 | `src/components/Preview/PreviewCanvas.tsx` | 20 个 useEffect，多个同类事件监听 | 合并同类事件监听，提取到自定义 hook |
| C-P1-6 | `src-tauri/src/db/mod.rs:78` | `lock().unwrap()` 可能在生产环境中 panic | 改用 `lock().map_err(...)` 优雅处理 |
| C-P1-7 | `src-tauri/src/commands/noise_reduction.rs` | 硬编码 `Command::new("ffmpeg")` 而非使用 `ffmpeg_binary()` | 统一使用 `ffmpeg_binary()` 获取 ffmpeg 路径 |
| C-P1-8 | `src-tauri/src/commands/media.rs` | 2216 行，多种职责混合 | 拆分为 `media-import.rs`、`media-analyze.rs`、`media-proxy.rs` |

### 依赖与安全 (4个)

| # | 位置 | 问题 | 修复建议 |
|---|------|------|----------|
| D-P1-1 | `package.json:55-61` | 7 个 Radix UI 依赖放在根 package.json 而非 apps/desktop | 移动到 `apps/desktop/package.json` 的 dependencies |
| D-P1-2 | `Cargo.toml:46` | `once_cell` crate 可用标准库 `std::sync::LazyLock` 替代（需 Rust 1.80+） | 提升 rust-version 到 1.80，替换为标准库 |
| D-P1-3 | `Cargo.toml:47` | `nnnoiseless` 引入了不必要的 `clap@3` 等传递依赖 | 检查是否支持 `default-features = false`，或寻找更轻量替代 |
| D-P1-4 | `Cargo.toml:41` | `zip` crate 在 Cargo.lock 中存在 v2 和 v4 两个大版本 | 升级直接依赖到 `zip = "4"`，统一版本 |

### 测试覆盖度 (5个)

| # | 位置 | 问题 | 修复建议 |
|---|------|------|----------|
| T-P1-1 | `editor-core/src/ai-smart-recommender.ts` | 覆盖率仅 4.1%（244行），无对应测试文件 | 创建测试文件覆盖推荐算法主要路径 |
| T-P1-2 | `editor-core/src/ai-speech-understanding.ts` | 覆盖率仅 12.1%（182行），无对应测试文件 | 创建测试文件覆盖语音理解核心逻辑 |
| T-P1-3 | `editor-core/src/color-grading/window-mask.ts` | 覆盖率仅 34.6%（81行），无对应测试文件 | 创建测试文件覆盖窗口遮罩逻辑 |
| T-P1-4 | `editor-core/src/audio/audio-mix-presets.ts` | 覆盖率仅 71.3%（115行），无对应测试文件 | 创建测试文件覆盖预设加载和应用逻辑 |
| T-P1-5 | `editor-core/src/color-grading/node-graph-engine.ts` | 覆盖率 76.0%（250行），分支覆盖率不足 | 补充测试用例覆盖未覆盖的分支路径 |

### 配置与文档一致性 (5个)

| # | 位置 | 问题 | 修复建议 |
|---|------|------|----------|
| F-P1-1 | `README.md:190` | 架构图中 `e2e/` 路径与实际不一致（实际在 `apps/desktop/e2e/`） | 修正架构图中的路径 |
| F-P1-2 | `docs/roadmap.md` | 路线图严重过时，停留在 v0.6.0，项目已达 v4.25.0 | 更新路线图反映 v4.x 里程碑 |
| F-P1-3 | `docs/product-plan.md:5` | 产品定位描述与实际不符（描述为轻量级工具，实际已是专业编辑器） | 更新产品定位文档 |
| F-P1-4 | `docs/product-plan.md:36-39` | Roadmap 仅规划到 v0.4 | 同步更新产品计划 |
| F-P1-5 | `.gitignore` | `docs/audit/` 和 `docs/evidence/` 目录未被 gitignore 覆盖 | 添加到 .gitignore |

---

## 四、P2 改进建议（可择机处理）

### 代码质量 (12个)

| # | 问题 |
|---|------|
| C-P2-1 | 全项目无 `React.memo` 使用，大量不必要的重渲染 |
| C-P2-2 | 缺少 `React.lazy` 代码分割，首屏加载体积大 |
| C-P2-3 | 生产代码 6 处 `as any` 类型断言 |
| C-P2-4 | `editor-core/src/model.ts` 2378 行，模型定义过于集中 |
| C-P2-5 | `src/components/MediaBin/MediaBin.tsx` 3198 行 |
| C-P2-6 | `backup.rs` 中非测试代码使用 `expect()` |
| C-P2-7 | `editor-core` 4 处 `as any` 类型断言 |
| C-P2-8 | 2 处 TODO 注释待清理 |
| C-P2-9 | 插件加载使用 `new Function`（安全风险，已有 DOMPurify 防护） |
| C-P2-10 | 3 处 `dangerouslySetInnerHTML`（已有 DOMPurify 防护） |
| C-P2-11 | `norm_path` 函数定义位置不当 |
| C-P2-12 | `Timeline.tsx` 中 `setTimeout` 清理不完整风险 |

### 依赖与安全 (6个)

| # | 问题 |
|---|------|
| D-P2-1 | Cargo.toml 版本声明过于宽泛（多数仅指定主版本号） |
| D-P2-2 | React 版本停留在 18.x（React 19 已发布） |
| D-P2-3 | Tailwind CSS 版本停留在 3.x（v4 已发布） |
| D-P2-4 | `aes-gcm` 版本较旧（0.10 vs 0.11） |
| D-P2-5 | `rusqlite` 可升级（0.31 vs 0.33） |
| D-P2-6 | Tauri 插件版本声明风格不一致（`^2` vs `^2.2.2`） |

### 测试覆盖度 (5个)

| # | 问题 |
|---|------|
| T-P2-1 | 3 个 AI 模块源文件无测试文件（emotion-analyzer, smart-recommender, speech-understanding） |
| T-P2-2 | 分支覆盖率整体偏低（87.7% vs 语句覆盖率 95.34%） |
| T-P2-3 | `preflight-checklist.spec.ts` 有 2 个测试失败 |
| T-P2-4 | `plugin-sdk` 包无任何测试 |
| T-P2-5 | `commands` 子目录核心业务逻辑无独立测试 |

### 配置与文档一致性 (6个)

| # | 问题 |
|---|------|
| F-P2-1 | 根目录残留 16 个临时/调试文件 | **已自动修复** ✅ |
| F-P2-2 | 插件 SDK 的 docs 脚本输出到被 gitignore 的目录 |
| F-P2-3 | AGENTS.md 与 CLAUDE.md 指令范围重叠但不冲突 |
| F-P2-4 | 子包版本号（0.6.0）与根包版本号（4.25.0）不一致 |
| F-P2-5 | CI e2e job 的 retries 设置与 Playwright 配置不一致 | **已自动修复** ✅ |
| F-P2-6 | CI e2e job 的 workers 设置本地与 CI 环境不同（设计如此） |

---

## 五、自动修复清单

以下低风险问题已自动修复并提交：

| # | 修复内容 | 文件 | 风险等级 |
|---|---------|------|---------|
| ✅ 1 | 移除错误的 npm 包 `desktop` | `package.json` | 低 |
| ✅ 2 | 修正更新端点 URL（GitHub 组织名错误） | `tauri.conf.json` | 低 |
| ✅ 3 | 添加 v4.25.0 版本比较链接 | `CHANGELOG.md` | 低 |
| ✅ 4 | 清理根目录 16 个临时文件 | 根目录 | 低 |
| ✅ 5 | 统一 CI retries 配置（移除命令行覆盖） | `.github/workflows/ci.yml` | 低 |

---

## 六、正面发现

审计中也发现了以下值得肯定的工程实践：

1. **Tauri Bridge 封装良好** — 所有 invoke 调用集中在 `tauri-bridge.ts`，符合架构规范
2. **路径安全验证完善** — `validate_path` 63处、`validate_path_for_write` 32处使用
3. **事件监听清理完整** — 组件卸载时正确清理事件监听器
4. **plugin-sdk 设计简洁** — 仅82行，类型完备性检查到位
5. **editor-core 覆盖率优秀** — 整体 95.34% 语句覆盖率，远超 80% 阈值
6. **E2E 测试规模大** — 270 个 spec 文件，~454 个测试用例，采用 POM 模式
7. **.gitignore 安全配置到位** — 正确排除了密钥、环境变量、构建产物
8. **Mock 基础设施完善** — `install-mocks.ts` 提供了完整的 Tauri bridge mock

---

## 七、优先修复路线图

### 立即处理（本周）
1. ~~移除错误的 npm 包 `desktop`~~ ✅ 已完成
2. ~~修正更新端点 URL~~ ✅ 已完成
3. ~~修复 CHANGELOG 缺失链接~~ ✅ 已完成
4. 修复 `Box::leak` 内存泄漏（ai.rs:288）
5. 修复 `DbPool` 连接池失效（db/mod.rs:65-74）
6. 恢复 2 个被跳过的 E2E 测试

### 短期处理（2周内）
1. 为 AI 模块（emotion-analyzer, smart-recommender, speech-understanding）补充单元测试
2. 修复 38 处空 catch 块和 40+ 处 `.catch(() => undefined)`
3. 将 Radix UI 依赖从根 package.json 移动到 apps/desktop
4. 统一 zip crate 版本（v2 → v4）

### 中期处理（1个月内）
1. 拆分 ffmpeg.rs（5203行）为多个模块
2. 拆分 Inspector.tsx（6420行）和 Timeline.tsx（6140行）
3. 更新 docs/roadmap.md 和 docs/product-plan.md
4. 补全 CHANGELOG 缺失的版本记录

### 长期规划
1. React 18 → 19 升级
2. Tailwind CSS 3 → 4 迁移
3. Rust 最低版本提升到 1.80+，移除 once_cell 依赖
4. 引入 React.memo 和 React.lazy 优化性能

---

*报告生成工具: ZCode AI Agent*
*审计代理: 代码质量审计 × 1, 依赖安全审计 × 1, 测试覆盖度审计 × 1, 配置文档审计 × 1*
