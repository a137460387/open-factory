{
  "name": "@open-factory/creator-dashboard",
  "version": "1.0.0",
  "description": "Creator dashboard for Open Factory creator program",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src --ext .ts",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "open-factory",
    "creator",
    "dashboard",
    "analytics",
    "revenue"
  ],
  "author": "Open Factory",
  "license": "MIT",
  "dependencies": {
    "date-fns": "^3.0.0",
    "decimal.js": "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^3.2.7",
    "@vitest/coverage-v8": "^1.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/open-factory/open-factory.git",
    "directory": "packages/creator-dashboard"
  }
}
